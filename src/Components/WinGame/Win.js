import React from 'react';
import BottomNavigation from '../Home/BottomNavigation';
import Game from './Game';

const Win = () => {
      
      return (
            <div>
                  <Game/>
                 <BottomNavigation/> 
            </div>
      );
};

export default Win;