import React from 'react';
import BottomNavigation from '../Home/BottomNavigation';

const Dashboard = () => {
      return (
            <div>
                    <BottomNavigation/>
            </div>
      );
};

export default Dashboard;